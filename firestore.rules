rules_version = '2';

// TEST MODE — open read/write for development (no auth required).
// Tighten before production!
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
